{
  "name": "deadbox",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "install": "cd deadbox-backend && npm install",
    "start": "cd deadbox-backend && npm install && npm start",
    "build": "cd deadbox-backend && npm install && cd ../deadbox-frontend && npm install"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "engines": {
    "node": "18.x"
  },
  "devDependencies": {
    "nodemon": "^3.1.9"
  }
}
